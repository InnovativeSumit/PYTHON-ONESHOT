data = {
    "name": "Sumit",
    "age": 20,
    "is_student": True
}

print(data)          # prints the dictionary
print(type(data))    # prints the type unordered,mutable,indexed,cannot contain duplicate keys
print(data["name"])